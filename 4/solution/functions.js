// Traditional function notation
function add(a, b) {
    return a + b;
}

// Arrow notation
add = (a, b) => {
    return a + b;
}

// Anonymous function
// (We've given it a name here, but no need to)
var anon = function(a, b) {
    return a + b;
}